---
apiVersion: v1
kind: Namespace
metadata:
  name: nids
  labels:
    # Required by the MutatingWebhookConfiguration namespaceSelector so the
    # Vault agent injector only targets this namespace.
    vault-injection: enabled
